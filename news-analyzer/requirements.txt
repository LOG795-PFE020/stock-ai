# Import base requirements
-r requirements-base.txt

# News analysis specific requirements
newspaper3k==0.2.8
nltk==3.8.1
transformers>=4.30.0
torch>=2.0.0
beautifulsoup4==4.12.3
aiohttp==3.9.5
lxml==4.9.3
# asyncio is now part of the Python standard library, don't need to install it separately
# Removed asyncio package as it's causing syntax errors and is part of the standard library
# uuid is part of the Python standard library, removing to avoid conflicts
numpy>=1.24.0
pandas>=2.0.0
pika>=1.3.0
fastapi>=0.95.0
uvicorn>=0.21.0
requests>=2.28.0
python-dotenv>=1.0.0
yfinance>=0.2.54
redis>=5.0.0
cachetools>=5.3.0
